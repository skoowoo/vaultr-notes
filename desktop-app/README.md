## Development

```bash
npm install
npm start
```

If dependency metadata was installed with the Electron binary download skipped,
fetch the binary when the network is stable:

```bash
npm rebuild electron
```
